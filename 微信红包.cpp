/*
class Gift {
public:
	int getValue(vector<int> gifts, int n) {
		map<int, int>m;
		for (int i = 0; i < n; i++)
		{
			++m[gifts[i]];
			if (m[gifts[i]] >= n / 2)
			{
				return gifts[i];
			}
		}
		return 0;
	}
};
*/